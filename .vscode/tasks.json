{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and run all",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "g++ ${workspaceFolder}/*.cpp -o ${workspaceFolder}/output && ${workspaceFolder}/output"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Compile all cpp files and run output executable in current folder"
    }
  ]
}
